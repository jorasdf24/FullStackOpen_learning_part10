const calculateBmi = (height: number, weight: number): string => {
  const bmiValue = weight / (height * height);
  switch (bmiValue) {
    case bmiValue > :
        
        break;
  
    default:
        break;
  }



};
